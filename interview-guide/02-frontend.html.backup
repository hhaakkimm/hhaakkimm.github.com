<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Frontend Development - Interview Guide</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.98);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 20px 60px rgba(0, 0000, 0.3);
        }

        h1 {
            color: #667eea;
            margin-bottom: 1rem;
            font-size: 2rem;
        }

        h2 {
            color: #764ba2;
            margin: 2rem 0 1rem;
            font-size: 1.5rem;
            border-bottom: 2px solid #e0e0e0;
            padding-bottom: 0.5rem;
        }

        h3 {
            color: #667eea;
            margin: 1.5rem 0 0.5rem;
            font-size: 1.2rem;
        }

        p,
        li {
            line-height: 1.8;
            color: #333;
            margin-bottom: 0 .5rem;
        }

        ul {
            margin-left: 2rem;
        }

        code {
            background: #f5f5f5;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            color: #e83e8c;
        }

        pre {
            background: #1e1e1e;
            color: #d4d4d4;
            padding: 1rem;
            border-radius: 8px;
            overflow-x: auto;
            margin: 1rem 0;
        }

        .back-btn {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            margin-bottom: 1.5rem;
        }

        .back-btn:hover {
            background: #764ba2;
        }

        .tip {
            background: #e7f3ff;
            border-left: 4px solid #2196F3;
            padding: 1rem;
            margin: 1rem 0;
            border-radius: 4px;
        }

        .important {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 1rem;
            margin: 1rem 0;
            border-radius: 4px;
        }
    </style>
</head>

<body>
    <div class="container">
        <a href="index.html" class="back-btn">← Back to Index</a>

        <h1>⚛️ Frontend Development: TypeScript & React 19</h1>

        <h2>TypeScript Fundamentals</h2>

        <h3>Core Types & Advanced Features</h3>
        <ul>
            <li><strong>Basic Types:</strong> string, number, boolean, array, tuple, enum, any, unknown, never, void
            </li>
            <li><strong>Interfaces vs Types:</strong>
                <pre>// Interface - can be extended and merged
interface Vulnerability {
    id: string;
    cveId: string;
    severity: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
}

// Type - more flexible
type VulnerabilityStatus = 'open' | 'in_progress' | 'resolved';
type VulnerabilityWithStatus = Vulnerability & { status: VulnerabilityStatus };</pre>
            </li>
            <li><strong>Generics:</strong>
                <pre>function getData&lt;T&gt;(url: string): Promise&lt;T&gt; {
    return fetch(url).then(res => res.json());
}

const vulnerabilities = await getData&lt;Vulnerability[]&gt;('/api/vulnerabilities');</pre>
            </li>
            <li><strong>Utility Types:</strong>
                <ul>
                    <li><code>Partial&lt;T&gt;</code> - All properties optional</li>
                    <li><code>Required&lt;T&gt;</code> - All properties required</li>
                    <li><code>Pick&lt;T, K&gt;</code> - Select specific properties</li>
                    <li><code>Omit&lt;T, K&gt;</code> - Exclude specific properties</li>
                    <li><code>Record&lt;K, T&gt;</code> - Object with specific keys</li>
                </ul>
            </li>
            <li><strong>Type Guards:</strong>
                <pre>function isVulnerability(data: unknown): data is Vulnerability {
    return typeof data === 'object' && data !== null && 'cveId' in data;
}</pre>
            </li>
        </ul>

        <h2>React 19 Deep Dive</h2>

        <h3>New Features in React 19</h3>
        <ul>
            <li><strong>React Compiler:</strong> Automatic memoization (no more manual useMemo/useCallback)</li>
            <li><strong>Actions:</strong> Automatic form handling with useActionState</li>
            <li><strong>useOptimistic:</strong> Optimistic UI updates
                <pre>const [optimisticVulns, addOptimisticVuln] = useOptimistic(
    vulnerabilities,
    (state, newVuln) => [...state, { ...newVuln, pending: true }]
);</pre>
            </li>
            <li><strong>use() hook:</strong> Read context and promises inside render</li>
            <li><strong>Document Metadata:</strong> <code>&lt;title&gt;</code>, <code>&lt;meta&gt;</code> in components
            </li>
            <li><strong>Server Components:</strong> React Server Components (if using Next.js)</li>
        </ul>

        <h3>React Hooks (Comprehensive)</h3>
        <ul>
            <li><strong>useState:</strong>
                <pre>const [filter, setFilter] = useState&lt;string&gt;('');
const [vulnerabilities, setVulnerabilities] = useState&lt;Vulnerability[]&gt;([]);</pre>
            </li>
            <li><strong>useEffect:</strong>
                <pre>useEffect(() => {
    const controller = new AbortController();
    
    fetch('/api/vulnerabilities', { signal: controller.signal })
        .then(res => res.json())
        .then(data => setVulnerabilities(data));
    
    return () => controller.abort(); // Cleanup
}, []);</pre>
            </li>
            <li><strong>useContext:</strong> Avoid prop drilling
                <pre>const ThemeContext = createContext&lt;'light' | 'dark'&gt;('light');
const theme = useContext(ThemeContext);</pre>
            </li>
            <li><strong>useReducer:</strong> Complex state logic
                <pre>type Action = 
    | { type: 'ADD_VULN'; payload: Vulnerability }
    | { type: 'REMOVE_VULN'; payload: string };

const [state, dispatch] = useReducer(reducer, initialState);</pre>
            </li>
            <li><strong>useRef:</strong> DOM access, mutable values
                <pre>const inputRef = useRef&lt;HTMLInputElement&gt;(null);
useEffect(() => { inputRef.current?.focus(); }, []);</pre>
            </li>
            <li><strong>useMemo & useCallback:</strong> Performance optimization (less needed in React 19)
                <pre>const sortedVulns = useMemo(() => 
    vulnerabilities.sort((a, b) => b.severity.localeCompare(a.severity)),
    [vulnerabilities]
);

const handleClick = useCallback(() => {
    console.log('clicked');
}, []);</pre>
            </li>
            <li><strong>Custom Hooks:</strong>
                <pre>function useVulnerabilities() {
    const [data, setData] = useState&lt;Vulnerability[]&gt;([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState&lt;Error | null&gt;(null);
    
    useEffect(() => {
        fetch('/api/vulnerabilities')
            .then(res => res.json())
            .then(setData)
            .catch(setError)
            .finally(() => setLoading(false));
    }, []);
    
    return { data, loading, error };
}</pre>
            </li>
        </ul>

        <h3>State Management</h3>
        <ul>
            <li><strong>Context API:</strong> Global state for small-medium apps</li>
            <li><strong>React Query / TanStack Query:</strong> Server state management
                <pre>import { useQuery } from '@tanstack/react-query';

function VulnerabilityList() {
    const { data, isLoading, error } = useQuery({
        queryKey: ['vulnerabilities'],
        queryFn: () => fetch('/api/vulnerabilities').then(res => res.json()),
        staleTime: 5 * 60 * 1000, // 5 minutes
    });
    
    if (isLoading) return &lt;Spinner /&gt;;
    if (error) return &lt;Error message={error.message} /&gt;;
    return &lt;List items={data} /&gt;;
}</pre>
            </li>
            <li><strong>Zustand:</strong> Lightweight state management
                <pre>import create from 'zustand';

const useStore = create((set) => ({
    filter: '',
    setFilter: (filter: string) => set({ filter }),
}));</pre>
            </li>
            <li><strong>Redux Toolkit:</strong> For complex enterprise apps (know the basics)</li>
        </ul>

        <h2>Tailwind CSS</h2>

        <h3>Core Concepts</h3>
        <ul>
            <li><strong>Utility-First:</strong> Build designs with utility classes
                <pre>&lt;div className="flex items-center justify-between bg-white rounded-lg shadow-md p-4"&gt;
    &lt;h2 className="text-xl font-bold text-gray-800"&gt;Vulnerability&lt;/h2&gt;
    &lt;span className="px-3 py-1 bg-red-500 text-white rounded-full text-sm"&gt;
        CRITICAL
    &lt;/span&gt;
&lt;/div&gt;</pre>
            </li>
            <li><strong>Responsive Design:</strong> <code>sm:</code>, <code>md:</code>, <code>lg:</code>,
                <code>xl:</code>, <code>2xl:</code>
                <pre>className="w-full md:w-1/2 lg:w-1/3"</pre>
            </li>
            <li><strong>Custom Configuration:</strong>
                <pre>// tailwind.config.js
module.exports = {
    theme: {
        extend: {
            colors: {
                brand: '#667eea',
            },
        },
    },
};</pre>
            </li>
            <li><strong>Dark Mode:</strong> <code>dark:</code> variant
                <pre>className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white"</pre>
            </li>
        </ul>

        <h2>Modern Frontend Architecture</h2>

        <h3>Component Design Patterns</h3>
        <ul>
            <li><strong>Compound Components:</strong> Related components working together</li>
            <li><strong>Render Props:</strong> Share code via props</li>
            <li><strong>Higher-Order Components (HOC):</strong> Wrapper components</li>
            <li><strong>Controlled vs Uncontrolled Components:</strong></li>
        </ul>

        <h3>Performance Optimization</h3>
        <ul>
            <li><strong>Code Splitting:</strong> <code>React.lazy()</code> and <code>Suspense</code>
                <pre>const VulnerabilityDetails = lazy(() => import('./VulnerabilityDetails'));

&lt;Suspense fallback={&lt;Spinner /&gt;}&gt;
    &lt;VulnerabilityDetails id={id} /&gt;
&lt;/Suspense&gt;</pre>
            </li>
            <li><strong>Virtualization:</strong> react-window, react-virtual for large lists</li>
            <li><strong>Image Optimization:</strong> Lazy loading, WebP format</li>
            <li><strong>Bundle Analysis:</strong> webpack-bundle-analyzer</li>
        </ul>

        <h3>Testing Frontend</h3>
        <ul>
            <li><strong>React Testing Library:</strong>
                <pre>import { render, screen, fireEvent } from '@testing-library/react';

test('filters vulnerabilities', () => {
    render(&lt;VulnerabilityList /&gt;);
    
    const input = screen.getByPlaceholderText('Search...');
    fireEvent.change(input, { target: { value: 'CVE-2024' } });
    
    expect(screen.getByText('CVE-2024-1234')).toBeInTheDocument();
});</pre>
            </li>
            <li><strong>Jest:</strong> Unit testing, mocking</li>
            <li><strong>Cypress/Playwright:</strong> E2E testing</li>
        </ul>

        <div class="important">
            <strong>⚠️ Interview Focus Areas:</strong>
            <ul>
                <li>Build a vulnerability dashboard component with filtering and sorting</li>
                <li>Explain how you'd handle real-time updates (WebSockets, SSE)</li>
                <li>Discuss performance for displaying 10,000+ vulnerabilities</li>
                <li>Know React 19 compiler and when manual optimization is still needed</li>
            </ul>
        </div>

        <h2>Common Interview Questions</h2>
        <ol>
            <li><strong>What's new in React 19?</strong> (Compiler, Actions, useOptimistic, use hook)</li>
            <li><strong>Explain the virtual DOM and reconciliation.</strong></li>
            <li><strong>When would you use useReducer instead of useState?</strong></li>
            <li><strong>How do you prevent unnecessary re-renders?</strong></li>
            <li><strong>Design a reusable data table component.</strong></li>
            <li><strong>Explain TypeScript's type inference.</strong></li>
            <li><strong>How would you implement infinite scrolling?</strong></li>
            <li><strong>What are the benefits of Tailwind over traditional CSS?</strong></li>
        </ol>

        <a href="index.html" class="back-btn">← Back to Index</a>
    </div>
</body>

</html>